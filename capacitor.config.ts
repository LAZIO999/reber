import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reber.learn.kurdish',
  appName: 'Reber Learn Kurdish',
  webDir: 'dist'
};

export default config;
